services:

  postgres:
    image: postgres:15

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: bookingdb

    ports:
      - "5432:5432"

  redis:
    image: redis:8

    container_name: booking-redis

    ports:
      - "6379:6379"